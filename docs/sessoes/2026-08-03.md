# 📓 Sessão — 2026-08-03

> Diário de trabalho do projeto CRM. Registro contínuo para retomada rápida.

## 🎯 Objetivo da sessão
Retomar o projeto (lendo ESTRATEGIA.md/PROGRESS.md), concluir pendências do bot, consertar a criação de clínica e o tema white-label, e cadastrar o 1º cliente real. Estabelecer registro por sessão.

## ✅ Feito
- **Verificado servidor**: containers Up/healthy, app local (:3110) + internet HTTP 200.
- **Bot WhatsApp publicado no GitHub** (`7bc1533`): webhook `/webhook/waha` + migration 010 + service `bots.ts`. Credenciais de admin sanitizadas dos docs. Removidos webhook mortos/duplicados. Remote corrigido p/ `crm-base`; identidade git = `medeirosinfra`.
- **Bot por clínica** (`0aa8fc5`): webhook resolve tenant pela `waha_sessao`; precedência keyword do tenant → geral do tenant → master.
- **Financeiro DRE** (`25ceaca`): `calcularDre()` + abas no módulo Financeiro.
- **Bot de botox/preenchimento + página persistida** (`ff5b711`): `/master/bots` agora usa o banco (antes mock local). Bots da Clínica Slim Body. Isolamento entre tenants validado.
- **Painel de Clientes Master** (`a550677`): "/master/clinicas" → "Clientes", KPIs, onboarding "como criar cliente em 3 passos". (`f8b217a`) CTA "+ Novo Cliente" na Visão do Ecossistema.
- **Fix criação de clínica** (`4c81dd4`, root cause): server-fn do TanStack falhava no Docker ("Seroval Error"). Criada rota customizada **`POST /api/clinicas`** no `src/server.ts` + `SUPABASE_URL` do server → Kong local. **Testado end-to-end.**
- **Fix tema white-label**: `1980e36` (gradientes), `394039f` (reaplica na troca de sessão — bug de timing), `7dbe70b` (fundo/superfícies derivam do matiz da cor do tenant).
- **Sistema zerado** e **1º cliente real cadastrado**: Clínica **Luana** (Harmonização Facial, cor azul `#1d96e2`, plano pro, sessão WAHA `Clinica Dr Luana`). Admin `medeirosinfra@gmail.com` validado no painel da clínica.

## 🧠 Decisões
- **Server-fn do TanStack não funciona no Docker** (preset node-server) → usar **rotas HTTP customizadas** no `src/server.ts` (mesma infra do webhook WAHA).
- **Banco principal = DB `supabase`** (tabelas completas). DB `crm_base` é órfão (3 tabelas).
- **Documentação agora com diário por data** em `docs/sessoes/` — um arquivo por sessão para retomar fácil.
- White-label deve refletir a identidade da clínica (cor no fundo, não só ícones).

## 💡 Ideias / próximos candidatos
- Criar rotas customizadas para as demais operações que precisem de server (padrão `/api/clinicas`).
- Campanhas com disparo real (fila + relatório).
- Anúncios com integração real (redes sociais).
- Subdomínio por clínica (DNS wildcard).
- Testes automatizados (Vitest).
- Screenshots no README do GitHub.
- Melhorar segurança do WAHA (mover envio para rota server).
- Novo cliente: cadastrar próximas clínicas conforme necessidade do usuário.

## ⚠️ Pendências
- Nenhuma bloqueante. Sistema com 1 cliente real ativo (Luana).

## 🔗 Estado ao final
- App no ar: `https://crm.medeirossolucoestech.com.br` (HTTP 200) e `http://172.16.0.50:3110`.
- `docker ps`: medeirosinfra-suite Up, waha Up, supabase-db healthy.
- DB: 1 tenant (Luana) + configuração de segmentos/módulos preservada.
- Repo: `medeirosinfra/crm-base` sincronizado (último commit `7dbe70b`).