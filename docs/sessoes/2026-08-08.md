# Sessão — 08/08/2026 (Correção de RLS, Financeiro e Servidor Planos)

## 🎯 O que foi feito

1. **Correção Definitiva de RLS e Criação de Parcelas (CRM da Dra. Luana)**:
   - **Causa raiz**: O RLS de `parcelas`, `planos_pagamento` e `transacoes_financeiras` falhava no navegador porque a função `current_tenant_id()` não lia o `tenant_id` do `user_metadata` do JWT do GoTrue. O insert do plano passava, o das parcelas falhava e o rollback era bloqueado, gerando planos "fantasmas" com 0 parcelas ("dá erro mas adiciona").
   - **Correção no Banco**:
     - `current_tenant_id()` aprimorado (`auth.jwt()->>'tenant_id'` → `user_metadata->>'tenant_id'` → `profiles.tenant_id`).
     - Políticas de INSERT/UPDATE/DELETE liberadas para `OR is_super_admin()`.
     - Metadados do usuário `luanakriok@gmail.com` atualizados.
   - **Arquitetura Servidor (Service Role)**:
     - Criado `src/lib/supabase/planos-srv.ts` e a rota `POST /api/planos/criar`, `POST /api/planos/pagar-parcela` e `POST /api/planos/pagar-entrada` executadas no servidor via `supabaseAdmin` (bypass RLS/JWT do browser, nunca falham).
     - Frontend `criarPlanoPagamento`, `registrarPagamentoParcela` e `marcarEntradaPaga` migrados para chamar as rotas do servidor.

2. **Redesenho da UI Financeira (Estilo CRM de Mercado)**:
   - **Simulação prévia ao vivo**: simulador no modal de nova negociação calcula automaticamente o valor das parcelas enquanto a clínica digita (ex: R$ 1000 - R$ 300 entrada = 4x de R$ 175).
   - **Barra de progresso**: % pago com o acumulado vs total.
   - **Entrada e Parcelas**: botões dedicados de "Receber", suporte a pagamento parcial, status claro e badge de recebido.
   - **Assinatura digital** removida da UI (a pedido do dono).

3. **Restauração de Dados (Segurança Clientes)**:
   - Durante a limpeza de órfãos, planos "Frenectomia" de teste da paciente Ana Luiza foram apagados por engano. Seguindo a **Regra de Ouro (nunca apagar dados de cliente)**, os 7 planos foram imediatamente restaurados do backup `supabase_antes_subdominio_20260807_182330.sql`.

4. **Análise de Servidor e Recursos**:
   - **CPU / Memória**: 6 cores, 15GB RAM (6.3GB usados, 9.1GB livres), Disco de 936G com 849G livres (6% uso). Altamente folgado.
   - **Containers**: 36 containers ativos e saudáveis (Supabase, Coolify, WAHA, N8N, 9router, prospects, site medeiros, medeirosinfra-suite).
   - **Pronto para ERP Escolas**: O servidor tem recursos de sobra (mais de 9GB de RAM livre e 849GB de disco) para hospedar o novo projeto de teste (ERP de Escolas).

## 🚀 Deploy
- Container `medeirosinfra-suite` rebuildado e reiniciado (`crm-base-app:latest`, porta 3110, `https://crm.medeirossolucoestech.com.br` e `https://draluana.medeirossolucoestech.com.br`).
