# Sessão 2026-08-07 — Subdomínio por cliente + Disparos agendados com IA

## Feito
- Subdomínio por cliente: migration 019 (`tenants.subdominio`), resolver 2-partes, endpoint `/api/tenant/:sub`, branding no login. Luana: `draluana.medeirossolucoestech.com.br`.
- Disparos agendados com IA: migration 018 (`campanhas` + janela/hora_exata/next_due), serviço `ia.ts` (9router), endpoints em `server.ts`. Testado geração + agendamento (sem disparo real).
- Proteção: backups + contagem antes/depois (8 pacientes, 9 agendamentos) — intactos.

## Pendências
- UI `/master/automacoes` (substituir ComingSoon) — falta finalizar.
- DNS wildcard no Cloudflare (dono) + rota no tunnel.
- Cron do host p/ `GET /api/disparos/devidos` a cada minuto.
